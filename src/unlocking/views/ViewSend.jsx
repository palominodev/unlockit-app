import { useEffect } from "react"
import { useDispatch } from "react-redux"

export const ViewSend = () => {
	const dispatch = useDispatch()
	useEffect(() => {

	}, [])
	
  return (
	<div>

	</div>
  )
}